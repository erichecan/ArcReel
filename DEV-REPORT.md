# DEV-REPORT：参考生视频「一键成片」

## 做了什么

在"参考生视频"工作流里新增了「一键成片」功能：把一集里已经生成好的每个视频单元 + 各自的旁白配音，按顺序自动拼接渲染成一条完整的 mp4，不再需要手动导出剪映草稿、再打开剪映桌面版渲染。原来的"导出剪映草稿"按钮照常保留，两个入口并存——一个是自动出成片，一个是给需要精修的人导出可编辑工程。

## 页面/功能变化

| 位置 | 变化 |
| :-- | :-- |
| 参考生视频画布顶部工具栏 | 新增「一键成片」按钮，紧邻「批量生成视频」。集里只要还有一个视频单元没生成成片，按钮就会置灰并提示原因；点击后进入渲染中状态 |
| 参考生视频画布（渲染完成后） | 顶部出现一条"成片预览"条：内嵌视频播放器 + 下载按钮；渲染中会显示进度提示 |
| 视频单元预览面板（此前已完成） | 每个单元新增删除按钮（本次会话早前已实现并部署） |

## 功能完成度

- ✅ 一键成片按钮：全有或全无判定（有单元没生成成片就不让点，服务端同样拒绝）
- ✅ 拼接渲染：按单元顺序拼接，每个单元的旁白替换该片段音轨（旁白短则补静音、长则截断，不会拉长总时长）
- ✅ 转场：沿用每个单元已有的转场设置（cut/fade/dissolve），不是纯生硬拼接
- ✅ 分辨率不一致自动处理：统一对齐到第一个单元的画面尺寸，不报错、不裁剪
- ✅ 渲染是独立的后台任务，不会跟视频生成任务抢机器资源（限制同时只能渲染 1 个）
- ✅ 成片可在线预览 + 下载
- ⚠️ 第一版不支持背景音乐（按之前约定，只拼视频+旁白，BGM 留到以后再做）
- ⚠️ 目前没有"脚本改了、旧成片已经过期"的自动提醒——如果某个单元后来又重新生成了成片，之前合成的那条不会自动失效或提示重新合成，需要自己判断要不要再点一次「一键成片」

## 已知问题 / 后续可以做的事

1. 没有过期提醒（见上）。工程上是可以做的（给成片单独登记一份"依赖了哪些单元版本"的记录），但这次范围没包含，先记录在这里。
2. 渲染是纯 CPU 工作（ffmpeg 转码），集数多、单元多时会比较慢，目前没有做进度百分比展示，只有"正在合成…"的笼统提示。
3. 只测过命令行层面的拼接逻辑（单元测试全过），没有用真实的多供应商视频片段跑过一次完整的端到端渲染验证画面观感（需要真实 ffmpeg 环境和几段真实视频文件，这次没做）。

## 需要你做的事：重新部署才能看到

跟上次删除按钮一样，这些改动只是改了代码，你现在跑着的 Docker 容器（`deploy-arcreel-1`，本机 `localhost:1241`）还是旧版本。要不要现在帮你重新构建镜像并重启容器？会中断当前容器里任何正在跑的生成任务（项目文件本身在挂载卷里，不会丢），而且跟上次一样，重启会让你当前的登录态失效，需要重新登录一次。
