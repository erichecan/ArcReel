# DEV-REPORT：ad 项目资产清单强制确认 + 美食脚本调研

## 做了什么

针对"探店/美食短视频生成的视频画面跨镜头对不上（厨师、锅、场景、菜品反复变化）"这个问题，做了三件事：

1. **广告/短片项目的"角色、场景、道具资产设计"从可选变成了必须做的一步**。之前这一步可以跳过，跳过后每个视频单元生成画面时都是"各自现编"，同一个厨师、同一口锅在不同镜头里就会长得不一样。现在写脚本前系统会先停下来，要求你登记好会反复出现的角色/场景/道具（或者明确告诉系统"这条短片不需要"），才能继续。
2. **内嵌创作 agent 现在能自己上网查证菜谱**。识别到脚本是具体菜品/菜谱内容时，agent 会先查一下这道菜真实的标准做法（比如有没有过油炸这类关键工序），确保写出来的脚本不会漏掉决定画面可信度的步骤。
3. **给这个新门禁配了一张前端确认卡片**，不用非得跟 agent 对话才能过这一步。

## 页面/功能变化

| 位置 | 变化 |
| :-- | :-- |
| 广告/短片项目 · 项目概览页 | 商品/brief 填完、卖点确认后，如果还没登记任何角色/场景/道具，会出现一张「资产清单确认」卡片：提示原因、可以直接跳到角色/场景/道具管理页去登记，也可以勾选"本项目不需要额外资产"后确认跳过 |
| 内嵌创作 agent 对话 | 进入这一步时 agent 会主动分析脚本要用到哪些反复出现的人物/场景/道具，引导你逐条确认 |
| 内嵌创作 agent（仅美食/菜谱类内容） | 生成剧本前会先自动查一下这道菜真实的制作流程，避免漏工序 |

## 功能完成度

- ✅ 资产清单确认门禁：脚本生成前必须确认，确认后才能继续；未登记任何资产时必须显式勾选"不需要"才能通过
- ✅ 前端确认卡片：可跳转资产管理页、可确认、有加载态和失败提示，三语（中/英/越）文案齐全
- ✅ 内嵌 agent 指令已同步：不会再误导 agent 说"可以跳过资产设计"
- ✅ 美食类内容自动联网查证菜谱工序（内嵌 agent 已开通 WebSearch 权限，仅在识别到具体菜品时触发）
- ⚠️ 这个确认是"一次性"的：确认过后，哪怕你之后又往项目里加了新角色，系统也不会提醒你"要不要重新确认"。目前没做这种二次提醒，第一版先这样，够用了再说
- ⚠️ WebSearch 权限是全局打开的（不止美食类项目能用），只是靠 agent 的行为指令把它管住只在美食场景触发——不是工具层面的硬限制，理论上如果指令没写好，其他类型项目的 agent 也有可能被诱导调用搜索，产生一点意料之外的费用（每 1000 次 $10，量很小）

## 已知问题 / 后续可以做的事

1. 资产清单确认没有"过期重新确认"机制（见上）。
2. WebSearch 只对美食类内容生效是靠 prompt 约束，不是硬性技术限制。
3. 这次是自动化测试全跑通（后端 12041 个用例、前端 2154 个用例都过），但没有用真实的探店/美食脚本重新走一遍完整生成流程去肉眼确认画面确实不再跨镜头乱跳——毕竟这依赖实际调用视频生成供应商，会产生真实费用，需要你自己找一条脚本试一次。

## 需要你做的事：重新部署才能看到

跟以前一样，这些改动只是改了代码，你现在跑着的 Docker 容器（本机 `localhost:1241`）还是旧版本。要不要现在帮你重新构建镜像并重启容器？会中断容器里正在跑的生成任务（项目文件在挂载卷里不会丢），重启后需要重新登录一次。
